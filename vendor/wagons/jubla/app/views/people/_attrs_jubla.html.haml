- if can?(:show_details, entry)
  = render_attrs(entry, :nationality, :profession, :bank_account, :additional_information)
  = render_attrs(entry, :ahv_number, :ahv_number_old, :j_s_number)
  = render_attrs(entry, :insurance_company, :insurance_number)
