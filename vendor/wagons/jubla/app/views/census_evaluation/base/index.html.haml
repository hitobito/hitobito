- @sheets = GroupSheet.new(self, @group)


= render 'shared/page_per_year'

= render 'groups/tabs'

#main.row-fluid
  -if @total
    -if @sub_groups
      %article.span6
        = render 'totals'
    %article.span6
      = render 'details'
      
  -else
    %p Für dieses Jahr sind keine Angaben vorhanden