- @sheets = GroupSheet.new(self, @group)
= render 'shared/page_per_year'
= render 'groups/tabs'

%table.table
  - entries.each do |event| 
    %tr
      %td
        %strong= link_to event.name, group_event_path(@group, event)
      %td= event.dates_full
      %td= event.booking_info
