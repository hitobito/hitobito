-#  Copyright (c) 2012-2013, Jungwacht Blauring Schweiz. This file is part of
-#  hitobito and licensed under the Affero General Public License version 3
-#  or later. See the COPYING file at the top-level directory or at
-#  https://github.com/hitobito/hitobito.

-title event.to_s

- content_for :toolbar do
  .btn-toolbar
    = action_button('TeilnehmerIn hinzufügen', new_group_event_participation_path(@group, @event, for_someone_else: true), :plus)

#main.row-fluid
  %article.span6
    %h2 Zugeteilte Teilnehmende
    %p{style: 'padding-top: 7px; margin-bottom: 20px;'}
      %span#booking_info= event.booking_info
      zugeteilt
    
    %div{style: "max-width: 550px;"}
      %table.table.table-striped.table-hover
        %tbody#participants
          - @participants.each do |p|
            = render 'participant', p: p
    

  %article.span6
    %h2 Anmeldungen
    = render 'prio_buttons'
    
    %div{style: "max-width: 600px;"}
      %table.table.table-striped.table-hover
        %tbody#applications
          - @applications.each do |p|
            = render 'application', p: p
