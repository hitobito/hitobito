%td
  %strong= link_to(p.person.to_s, group_event_participation_path(@group, @event, p.id))
  %br/
  = p.person.town
  %br/
  / application date & best guess at Group::State (2nd in hierarchy)
  .muted= [l(p.created_at.to_date, format: :long), p.person.layer_groups[1]].compact.join(', ')

%td= p.priority(event) if event.priorization?
%td= p.confirmation if event.requires_approval?
