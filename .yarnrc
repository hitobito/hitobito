# The asset build itself lives in devDependencies (esbuild, sass, ...), so
# `yarn install` must not prune them just because NODE_ENV is production - which
# it is while the production image builds the assets (see the Dockerfile).
--install.production false
