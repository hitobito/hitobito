## Refactoring

This workflow should not change the functionality, but improve the internal structure. The goal could be a future extension, performance gains or better maintenance.

As such, the addition of specs is optional. Without new specs, nothing may fail at first. For added abstractions, they new specs still allowed and needed.

Otherwise, the development workflow above still applies.
