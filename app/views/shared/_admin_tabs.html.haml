-#  Copyright (c) 2012-2013, Jungwacht Blauring Schweiz. This file is part of
-#  hitobito and licensed under the Affero General Public License version 3
-#  or later. See the COPYING file at the top-level directory or at
-#  https://github.com/hitobito/hitobito.

- bar.tab "Kursarten", event_kinds_path, %w(event_kinds) if can?(:index, Event::Kind)
- bar.tab "Qualifikationsarten", qualification_kinds_path, %w(qualification_kinds) if can?(:index, QualificationKind)

= render_extensions(:admin_tabs_kinds, locals: { bar: bar })

- bar.tab "Texte", custom_contents_path, %w(custom_contents) if can?(:index, CustomContent)
- bar.tab "Etikettenformate", label_formats_path, %w(label_formats) if can?(:index, LabelFormat)

= render_extensions(:admin_tabs_end, locals: { bar: bar })

