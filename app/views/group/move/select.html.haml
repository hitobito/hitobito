-#  Copyright (c) 2012-2013, Jungwacht Blauring Schweiz. This file is part of
-#  hitobito and licensed under the Affero General Public License version 3
-#  or later. See the COPYING file at the top-level directory or at
-#  https://github.com/hitobito/hitobito.

- title t('.title')

%p= t('.select_group')

= standard_form(:move, url: move_group_path(@group), builder: PlainObjectFormBuilder) do |f|
  - @candidates.each do |label, values|
    = field_set_tag label do
      - values.each do |candidate|
        = f.inline_radio_button(:target_group_id, candidate.id, candidate.to_s, false)

  = form_buttons(f, submit_label: t('.title'), cancel_url: group_path(@group))
