#!/usr/bin/env bash

psql_sql_cmd() {
  CMD="psql -U hitobito -d hitobito_test -c \"$1\""
  docker compose exec postgres bash -c "$CMD"
}

setup_test_db() {
  psql_sql_cmd "DROP DATABASE IF EXISTS $1;"
  psql_sql_cmd "CREATE DATABASE $1;"
}
