language: ruby
env:
  global:
    - HEADLESS=true
    - RAILS_DB_ADAPTER=mysql2
rvm:
  - 1.9.3
  - 2.0
  - 2.1
  - 2.2
  - 2.3
  - 2.4
install:
  - sed -i "s/^\(gem .mysql2.\),.*$/\1/" Gemfile
  - bundle update mysql2 --path vendor/bundle
  - bundle install --path vendor/bundle
script:
  - bundle exec rake db:create ci --trace
