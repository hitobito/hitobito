= dropdown_button('Person hinzufügen', @group.possible_role_links, :plus) if can?(:new, @group.roles.new)

= button_group do
  = action_button('List importieren', new_group_csv_imports_path, :download) if can?(:new, @group.roles.new)
  = action_button('Export', '#', :upload)
    
= action_button('Drucken', '#', :print)

-#.pull-right
  -#= dropdown_button(@group.filter_name, @group.people_filter_links, nil, nil)
= pill_dropdown_button(@group.filter_name, @group.people_filter_links, nil, nil, 'pull-right')
