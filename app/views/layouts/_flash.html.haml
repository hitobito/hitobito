- if flash[level].present?
  #flash
    %div{:class => "alert alert-#{flash_class(level)}"}
      %a.close{:'data-dismiss' => 'alert', :href => '#'} &times;
      = simple_format([flash[level]].flatten.join("\n"))
