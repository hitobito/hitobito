= entry_form(buttons_bottom: false) do |f|
  = f.labeled_input_fields :label, :description
  = f.labeled_input_field(:validity, help_inline: 'Jahre')
  = f.labeled_input_field(:reactivateable, help_inline: 'Jahre')
