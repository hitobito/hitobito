#tag-lists-available.modal.fade
  .modal-dialog
    .modal-content
      .modal-header
        %h5.bold.modal-title
          %b= title

      = standard_form(:tag_list,
        url: url, method: method) do |f|
        .modal-body
          .row-fluid
            = hidden_field_tag('ids', people_ids)
            = yield

            .checkboxes
              - if tags.present?
                = available_tags_checkboxes(tags)
              - else
                = t('.no_entries')
        .modal-footer
          = submit_button(f, submit_label)
          = link_to(ti('button.cancel'), '#',
            class: 'link cancel',
            onclick: "event.preventDefault(); $('#tag-lists-available').modal('hide')")
