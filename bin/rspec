#!/usr/bin/env ruby

ENV['WAGONS'] = ''
ENV['RAILS_ENV'] = 'test'
# ENV['RAILS_TEST_DB_NAME'] = 'hit_core_test'

# Ensure that schema.rb is current for the test-db to load
# system('bundle exec rake db:migrate')

if ENV['DISABLE_SPRING']
  system("bundle exec rspec #{ARGV.join(' ')}")
else
  system("bundle exec spring rspec #{ARGV.join(' ')}")
end
