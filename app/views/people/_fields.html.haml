-#  Copyright (c) 2012-2013, Jungwacht Blauring Schweiz. This file is part of
-#  hitobito and licensed under the Affero General Public License version 3
-#  or later. See the COPYING file at the top-level directory or at
-#  https://github.com/hitobito/hitobito.

= field_set_tag do
  = f.labeled_input_fields :first_name, :last_name, :nickname, :company_name, :company

  = render_extensions :name_fields, locals: { f: f }

= render 'contactable/fields', f: f

= f.labeled(:people_relations) do
  #people_relations_fields_blueprint{style: 'display: none', data: { blueprint: people_relation_fields_blueprint } }

  #people_relations_fields
    = link_to 'Eintrag hinzufügen', 'javascript:$("#people_relations__tail").each(Application.setupEntityTypeahead);',
        class: 'add_nested_fields', data: { association: 'people_relations', 'blueprint-id' => 'people_relations_fields_blueprint' }

= field_set_tag do
  = f.labeled(:gender) do
    - [:m, :w, ''].each do |key|
      = f.inline_radio_button(:gender, key, gender_label(key))

  = f.labeled_string_field(:birthday,
                           value: f.date_value(:birthday),
                           help_inline: t('.format_birthday'),
                           class: 'span2')

= render_extensions :fields, locals: { f: f }

= field_set_tag do
  = f.labeled_input_field(:additional_information, help: t('.no_sensitive_information'))
