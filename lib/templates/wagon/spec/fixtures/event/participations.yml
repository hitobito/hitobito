top_leader:
  event: top_course
  participant: leader
  active: true

top_course_applicant:
  event: top_course
  participant: member
  active: false
  application: top_course_applicant

top_participant:
  event: top_event
  participant: member
  active: true
  application: top_participant
