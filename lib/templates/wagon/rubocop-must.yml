
# Rubocop configuration that MUST hold for every build.

inherit_from: ../hitobito/rubocop-must.yml

AllCops:
  Exclude:
    - db/**/*
    - config/**/*
    - bin/**/*
    - spec/**/*
    - test/**/*
