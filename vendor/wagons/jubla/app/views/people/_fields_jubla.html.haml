= field_set_tag do
  = f.labeled_input_fields(:name_mother, :name_father)

= field_set_tag do
  = f.labeled(:gender) do
    = entry.radio_for_gender(f,:m)
    = entry.radio_for_gender(f,:w)
    = entry.radio_for_gender(f,:u)

  = f.labeled_input_field(:birthday, value: f(entry.birthday))

= field_set_tag do
  = f.labeled_input_fields(:nationality, :profession, :bank_account, :additional_information)

= field_set_tag do
  = f.labeled_input_fields(:ahv_number, :ahv_number_old, :j_s_number)

= field_set_tag do
  = f.labeled_input_fields(:insurance_company, :insurance_number)
