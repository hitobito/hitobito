-#  Copyright (c) 2023, Schweizer Wanderwege. This file is part of
-#  hitobito and licensed under the Affero General Public License version 3
-#  or later. See the COPYING file at the top-level directory or at
-#  https://github.com/hitobito/hitobito.

%p= t('.body.greeting', name: @resource.full_name)

- if @resource.encrypted_password.present?
  %p= t('.body.reset_instructions.with_password', application_name: Settings.application.name)
- else
  %p= t('.body.reset_instructions.without_password_html', application_name: Settings.application.name)

%p= link_to edit_password_url(@resource, reset_password_token: @token),
            edit_password_url(@resource, reset_password_token: @token)

- if @resource.encrypted_password.present?
  %p= t('.body.ignore_information_html')
