#!/usr/bin/env ruby

require_relative "../config/boot"
require "rails"
require_relative "../config/environment"
Rails.application.eager_load!


if ARGV[0].present?
  puts Patches::Analyzer.new(ARGV[0].constantize).patches
else
  puts "Please pass Class to analze"
end
