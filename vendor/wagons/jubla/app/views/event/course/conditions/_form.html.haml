= include_wysiwyg_assets
- parent_sheet(GroupSheet.new(self, group, :group_path))
= entry_form(cancel_url: group_event_course_conditions_path(group)) do |f|
  = f.labeled_input_field :label
  = f.labeled_text_area :content, class: 'span8 wysiwyg', rows: 20

